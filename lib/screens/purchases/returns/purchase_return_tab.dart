import 'package:flutter/material.dart';

class PurchaseReturnTab extends StatelessWidget {
  const PurchaseReturnTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'شاشة مرتجعات المشتريات',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}