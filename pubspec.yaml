name: accounting_desktop
description: Static Accounting Desktop UI
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  font_awesome_flutter: ^10.6.0
  google_fonts: ^6.1.0
  flutter_bloc: ^9.1.1

flutter:
  uses-material-design: true